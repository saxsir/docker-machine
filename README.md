# docker-machine
docker環境を切り替えるだけのリポジトリ

ghq + pecoで移動したいがために`.envrc`だけ置いたディレクトリを置く。  
このリポジトリだけcloneしてもローカルにdocker-machineが入っている & defaultの仮想マシンを作成済みじゃないと意味ない。

## Requirements
```
$ direnv --help
direnv v2.7.0
Usage: direnv COMMAND [...ARGS]

...
```
